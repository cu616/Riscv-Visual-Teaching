# 自动化开发执行记录

更新时间：2026-05-04

> 状态说明：本文是阶段执行记录，记录当时自动化开发批次的产物、测试和剩余风险。它用于复盘和追溯，不等同于当前最新路线。当前路线请先看 [文档目录向导](README.md) 和 [文档状态与路线索引](文档状态与路线索引.md)。

补充记录：2026-05-05 已新增 OpenHarmony / 香橙派 ArkWeb 移植原型，单独放在 `openharmony-port/`。

## 1. 当前自动化批次目标

执行长期任务：

```text
docs/长期自动化开发任务_非Blockly主线工程化.md
```

当前批次聚焦：

- 主线一键打开稳定性。
- 非 Blockly 自研积木主线模块拆分。
- 自由小积木与吸附模型工程化。
- 保存/导入格式可靠化。
- 明早验收和问题定位。

## 2. 已完成代码产物

### 启动

- `打开非Blockly自研积木桌面版.bat`
- `scripts/launch_non_blockly_app.ps1`

### 主线模块

- `app/src/datapath.js`
- `app/src/ui-utils.js`
- `app/src/case-format.js`
- `app/src/operand-model.js`
- `app/src/machine-state.js`
- `app/server.mjs` 支持被自动化脚本导入启动。
- `app/smoke-http.mjs` 新增轻量 HTTP 烟测。

### 主线 UI

- `app/index.html` 增加“保存案例 / 导入案例”。
- `app/styles.css` 增加自由小积木、状态初始化、教学备注和立即数浮层编辑器样式。
- `app/src/app.js` 接入自由小积木、吸附、高亮、拖出、保存导入、立即数浮层编辑、初始状态编辑和教学备注。
- `app/src/app.js` 接入分步调试历史，支持“上一步 / 下一步 / 暂停 / 重置”控制组。
- 当前执行到的指令积木会以边缘高亮提示。
- 标签帽贴到指令积木后保持标签小积木自身尺寸，不再被停靠虚线框拉长。
- 工具栏新增执行进度显示。
- 自动执行时会临时锁住编辑区、素材区和初始状态编辑区，避免运行中误改导致状态不同步。
- 指令拖拽时会显示“插入为第 N 步”，靠近主堆叠列释放后自动整理成上下步骤栈。
- 指令积木发生明显重叠时才会自动整理；普通上下拖动保持自由位置。
- 自动整理后的指令间距已收紧，更接近实体积木贴合成条。
- 单步执行高亮框已改为贴住指令头本体，不再包住过大的判定区域。
- 自由小积木拖拽时右下角出现删除区，拖入删除区即可删除。
- 自由小积木支持右键选中、Ctrl 追加/取消选中，并可用 Delete / Backspace 删除选中项。
- 调试工具栏新增“上一步只回退执行状态”的显式提示。
- 教学备注已从单一文本框升级为案例标题、教学目标、讲解步骤三段结构。
- `.riscvteach.json` 的 `notes` 字段已支持结构化对象，并兼容旧版字符串备注。
- HTTP 烟测已覆盖调试提示、结构化备注输入框。
- 编辑区新增右上角视图工具组，支持 `- / 100% / +` 缩放，并把机器状态、汇编预览、教学说明作为按需打开的辅助视图入口。
- 工作区布局从“编辑区 + 固定右栏”收敛为“素材区 + 编辑区”主视图；机器状态改为右侧浮动抽屉，默认不占拼接空间。
- 汇编/JSON 预览改为编辑区角落按钮打开的浮动面板，避免继续固定在底部不可见区域。
- 教学说明改为默认窄条折叠状态，需要时展开为浮动说明面板。
- OpenHarmony 移植版同步脚本会继续从 `app/` 同步这些 UI 变化，并保留隐藏数据流动画的移植补丁。

### OpenHarmony 移植原型

- `openharmony-port/` 单独承载 OpenHarmony ArkTS 壳，不直接混入 `app/` 主线目录。
- `openharmony-port/entry/src/main/ets/pages/Index.ets` 使用 ArkWeb 加载 `$rawfile('app/index.html')`。
- `openharmony-port/entry/src/main/resources/rawfile/app/` 保存从 `app/` 静态资源化得到的页面、样式和脚本。
- `openharmony-port/entry/src/main/resources/rawfile/app/src/datapath.js` 替换为轻量占位实现，可见数据流动画区域已在移植 CSS 中隐藏。
- `openharmony-port/entry/src/main/resources/rawfile/app/openharmony-bridge.js` 预留 Web 层桥接入口。
- ArkTS 侧 `OpenHarmonyBridge` 已预留 `getRuntimeInfo`、`saveCase`、`loadCase` 方法；其中保存/导入暂时返回“原生文件接口待实现”，用于后续接入 OpenHarmony 文件选择器和持久化能力。
- `openharmony-port/scripts/sync-rawfile.mjs` 新增静态资源同步脚本，可从 `app/` 重建 rawfile 页面并自动恢复移植版补丁。
- `openharmony-port/scripts/smoke-openharmony-port.mjs` 新增 rawfile 结构烟测，检查 ArkTS 入口、rawfile 页面、桥接脚本、数据流占位、AppScope 资源和 OpenHarmony SDK 字段类型是否齐备。
- `openharmony-port/build-profile.json5` 已按当前 OpenHarmony SDK API 24 修正：`runtimeOS` 为 `OpenHarmony` 时，`compileSdkVersion`、`compatibleSdkVersion`、`targetSdkVersion` 均使用数值 `24`。
- `openharmony-port/AppScope/resources/base/` 已参考 `ohos_electron_hap/` 补齐应用级字符串、图标和 configuration，避免 DevEco 索引 AppScope 资源时失败。
- `docs/OrangePi_RV2_X1_用户手册_v1.1.pdf`、`docs/OPI RV2 V1_1_SCH_20250508(1).pdf`、`docs/Ky X1芯片手册.pdf` 已加入仓库作为 RV2 / X1 硬件资料参考。
- `ohos_electron_hap/` 已加入仓库作为 OpenHarmony/HarmonyOS Electron HAP 工程结构参考；当前仅借鉴构建配置和 AppScope 组织方式，不切换主线方案。

## 3. 已完成文档产物

- `docs/明早验收与问题定位清单.md`
- `docs/长期自动化开发任务_非Blockly主线工程化.md`
- `docs/夜间自动化推进计划.md`
- `docs/自动化开发执行记录.md`

同时更新：

- `README.md`
- `docs/工程目录与代码分区说明.md`
- `docs/项目进度与结构总结.md`
- `docs/下一阶段版本计划.md`
- `docs/技术路线决策与阶段复盘.md`
- `openharmony-port/README.md`
- `openharmony-port/docs/移植记录.md`
- `openharmony-port/docs/HDC部署检查清单.md`
- `openharmony-port/docs/OH开发经验与AI接手须知.md`

## 4. 已完成测试覆盖

`app/test-core.mjs` 已覆盖：

- 指令解析与执行。
- 标签跳转。
- 扩展指令执行。
- `x0` 恒零。
- `shamt` 范围校验。
- `.riscvteach.json` 保存格式 round-trip。
- `initialState` 和 `notes` 保存格式预留字段。
- 机器初始状态模型：
  - 过滤非法寄存器。
  - 保持 `x0` 恒零。
  - 归一化寄存器和内存整数。
  - 从初始状态创建可执行机器状态。
  - 写入和清除单个初始值。
- 自由小积木模型：
  - 创建自由小积木。
  - 拖出后清空源字段。
  - 吸附后写入目标字段。
  - 导入归一化。

## 5. 当前验证命令

已通过：

```powershell
npm.cmd run check
npm.cmd test
npm.cmd run smoke
npm.cmd run oh:sync
npm.cmd run oh:smoke
npm.cmd run oh:check
```

`npm.cmd run smoke` 会在独立端口启动 `app/` 静态服务，检查首页关键 UI 锚点，并拉取首页引用的 CSS 与 JS 资源。它不依赖浏览器自动化库，适合作为无人值守开发的第一层白屏排查。

`npm.cmd run oh:smoke` 检查 OpenHarmony 移植目录的静态结构，不等价于 DevEco Studio 构建或香橙派真机运行。真正的 ArkWeb 渲染、JSBridge 调用和 HDC 安装仍需要在 OpenHarmony 开发环境中验证。

## 6. 当前剩余风险

- 自由小积木吸附手感需要人工视觉反馈。
- 立即数双击编辑已改为页面内浮层输入，但仍需要人工确认位置和手感。
- 初始状态和教学备注已经进入保存导入闭环，但仍需要人工用真实案例确认演示体验。
- “上一步”当前回退的是机器执行快照，不是撤销积木编辑。
- 标签帽、指令外框、机器状态网格已按最新反馈调整，仍需要人工看视觉比例。
- 运行时编辑锁是第一版，后续如果需要可细分为“允许滚动但不允许拖拽/编辑”。
- 自动整理步骤栈会把主列附近释放的指令统一排到 `x=36`，如果后续需要自由布局，可增加开关。
- 指令重叠整理是第一版网格化规则，当前只在明显重叠时触发，复杂布局下可能还需要按实际手感调整行距和列距。
- 结构化备注已保存导入，但需要用真实课堂案例确认字段名称是否顺手。
- `app/src/app.js` 仍包含 DOM 事件绑定和吸附目标查找，后续可继续拆分。
- 当前 HTTP 烟测只能覆盖服务启动、HTML 主结构和静态资源 200，不能替代真实拖拽手感验收。
- Electron 原生窗口仍未修复，当前稳定入口是 Edge / Chrome 应用窗口模式。
- OpenHarmony 移植版尚未接入原生文件保存/导入；当前只是 JSBridge 方法预留。
- OpenHarmony 移植版尚未在香橙派真机通过 HDC 部署验证；需要后续用 DevEco Studio 或 hvigor + hdc 完成构建、签名、安装和日志排查。

## 7. 明早建议先看

```text
docs/明早验收与问题定位清单.md
```

优先反馈：

1. 吸附是否灵敏。
2. 拖出是否直观。
3. 保存/导入是否恢复完整。
4. 一键打开是否像应用窗口。

## 8. 2026-05-10 OpenHarmony 1080p 移植执行记录

本轮围绕香橙派 OpenHarmony 真机上的 1920x1080 显示环境，继续推进 `app/` 主线到 `openharmony-port/` 的 ArkWeb rawfile 移植。

已完成：

- 顶部工具栏重新整理：
  - 分步执行、暂停、重置、自动执行使用白底线性图标。
  - 清空、保存、导入、OpenHarmony 展示等命令保留短文字。
  - 移除独立“演示模式”按钮，课堂展示聚焦 OpenHarmony 展示和画布缩放。
- 工作区布局调整：
  - 指令积木选择区与指令编辑区改为左右相邻，避免素材栏覆盖编辑画布。
  - 指令编辑区标题栏独立占位，网格画布从标题栏下沿开始。
  - 左侧素材栏、右侧辅助栏在 OH 版保留拖拽调宽。
- 日志区调整：
  - 执行日志与教学反馈改为底部抽屉。
  - 默认收起，通过底部小三角展开/收起。
- OpenHarmony 展示调整：
  - “香橙派 / 软总线”从画布伪元素改为 `softbus-canvas-anchor` 真实 DOM。
  - 该锚点位于 `instruction-list` 缩放层内，可随积木缩放和平移。
  - 重置图标方向修正为向右回转。
- 运行时诊断：
  - `OpenHarmonyBridge.getRuntimeInfo()` 返回 `targetDisplay: '1920x1080'`。
  - 页面顶部显示 `OH 1080p`，悬停可查看当前实际视口。
- OpenHarmony smoke 加强：
  - 检查新版 UI 锚点、日志抽屉、运行时标识、软总线锚点和 1080p bridge 信息。

本轮验证已通过：

```powershell
npm.cmd run check
npm.cmd test
npm.cmd run oh:check
```

后续真机重点：

1. 观察顶部工具栏是否仍有横向滑动。
2. 检查编辑区和素材栏是否完全不重叠。
3. 验证软总线锚点随画布缩放同步变化。
4. 验证底部日志抽屉展开/收起手感。

## 9. 2026-05-11 UI 交互与 Windows 桌面同步记录

本轮根据 OpenHarmony 真机反馈继续调整了主线 `app/` 与 `openharmony-port/` rawfile，重点不是新增教学内容，而是把已经暴露出的交互问题整理成可复用的产品改动。

已经形成文档：

```text
docs/2026-05-11_UI交互改动与Windows桌面版同步说明.md
```

本轮功能变动摘要：

- OpenHarmony 展示不再与普通数据执行动画互相覆盖。关闭 OH 展示时自动移除 OH 卡片；同时开启 OH 展示和自动执行时，OH 动画与数据动画在辅助栏内上下共存。
- 重置命令合并为同时重置机器执行状态与 OH 展示进度，避免展示模式打开后无法回到初始状态。
- 数据演示卡片增加目标引导，明确显示“寄存器 xN / 存储器[addr] / PC”的变化，减少只看到数值箭头时的理解成本。
- 辅助栏增加高度调节，内部滚动层修复为可上下滚动；数据演示卡片支持拖动调整位置，便于在课堂展示时避开主要积木区。
- 新增编辑撤销功能，覆盖积木添加、删除、移动、填槽、导入、清空等编辑动作；它与“上步”分离，后者仍然只回退 CPU 执行状态。
- 页面高度布局从散落的固定高度修正为整页 flex 结构，修复下方白条和部分区域白屏问题。
- 重新测试完整拖拽动画后，OpenHarmony 仍会闪退，因此已退回兼容版拖拽动画。Windows 桌面版后续可以继续保留完整拖拽体验，OH 侧保持稳定优先。

本轮验证：

```powershell
npm.cmd run check
npm.cmd test
npm.cmd run oh:check
```

这份记录用于提醒后续维护者：不要只把改动理解成 OpenHarmony 适配，其中的撤销、辅助栏、数据卡片说明、展示/执行解耦，都应作为 Windows 桌面版体验优化的候选项。

## 10. 2026-05-11 数据动画与机器状态面板修正记录

本轮继续围绕课堂展示体验和 OpenHarmony 真机反馈修正 UI：

- 数据动画新增暂停/继续，暂停按钮在动画播放中会冻结当前帧，再次点击继续播放。
- 速度档位删除旧 `0.5x`，改为 `1x / 1.5x / 2x`；新的 `1x` 是更慢的课堂观察速度。
- 数据变化从单个数值 badge 改为卡片：寄存器/存储器变化与 PC 变化可左右并排同时出现。
- 机器状态中的“内存”文案统一改为“存储器”，源码字段仍保留 `memory`。
- 机器状态初始化区调整为五项同排：`寄存器/存储器 | 目标编号 | 数值 | 写入 | 清除`。
- 真机上五项未并排的根因已定位：`openharmony-port.css` 后加载并覆盖了主样式中的 `.state-editor-row`，原规则把它强制成 `1fr 1fr` 两列。当前已在 OH 覆盖层同步为五列 grid。

关键文件：

```text
app/index.html
app/styles.css
app/src/app.js
app/src/state-animation.js
openharmony-port/entry/src/main/resources/rawfile/app/index.html
openharmony-port/entry/src/main/resources/rawfile/app/styles.css
openharmony-port/entry/src/main/resources/rawfile/app/openharmony-port.css
openharmony-port/entry/src/main/resources/rawfile/app/src/app.js
openharmony-port/entry/src/main/resources/rawfile/app/src/state-animation.js
docs/2026-05-11_UI交互改动与Windows桌面版同步说明.md
```

验证命令：

```powershell
npm.cmd run check
npm.cmd test
npm.cmd run oh:check
npm.cmd run smoke
```

后续接手注意：如果 OH 真机表现和 Web 主线不一致，不要只查 `app/styles.css`，还要检查后加载的 `openharmony-port.css` 是否存在覆盖。
